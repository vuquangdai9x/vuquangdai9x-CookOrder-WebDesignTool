{
  "_source": "CookOrder GDD New - MapLevelProgress: title row 1, category row 2, headers row 3, data begins at row 4. Each data row identifies itself through Map (A) and Level (B); map ranges and level counts are intentionally not configured.",
  "_note": "Column mapping for the node-graph Remote Data tab. The current shared sheet stores graph-format level strings in MapLevelProgress. Column indexes are 0-based (0=A, 1=B, ...), and the tab UI can override them at runtime.",
  "tabName": "MapLevelProgress",
  "startRow": 4,
  "columns": {
    "map": 0,
    "level": 1,
    "ingredientWeights": 3,
    "customerDishesSequence": 4,
    "complexityCurve": 5,
    "shuffleCurve": 6,
    "customerString": 15,
    "gridString": 16,
    "queueString": 17
  }
}
