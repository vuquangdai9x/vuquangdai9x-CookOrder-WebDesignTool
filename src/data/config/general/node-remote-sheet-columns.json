{
  "_note": "Column mapping for the NODE-GRAPH Remote Data tab. Same spreadsheet as the legacy tab, DIFFERENT sheet tab, so the two systems never overwrite each other: node level strings speak the graph's id space and a bracket dish grammar the legacy tool cannot parse. Column indexes are 0-based (0=A, 1=B, ...), and the tab's own UI can override each of them at runtime.",
  "tabName": "NodeLevelProgress",
  "columns": {
    "map": 0,
    "level": 1,
    "ingredientWeights": 3,
    "customerDishesSequence": 4,
    "complexityCurve": 5,
    "shuffleCurve": 6,
    "customerString": 15,
    "gridString": 16,
    "queueString": 17
  }
}
